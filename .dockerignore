# Installed/rebuilt inside the image
node_modules
**/node_modules

# Build outputs (regenerated by `pnpm build`)
.next
.open-next
.wrangler
test-results
tsconfig.tsbuildinfo

# VCS / CI / editor / agent
.git
.github
.vscode
.claude

# Dev-only configs & tests not used by the Next.js build
e2e
playwright.config.ts
.lefthook.yml
commitlint.config.cjs
renovate.json

# Docs & meta
docs
*.md
LICENSE

# Local env (never bake secrets into an image)
.env
.env.*
